# Type of Arguments
    # Default Arguments
    # Positional Arguments
    # Keyword Arguments

# Default Argument ->   Help us to handel errors
def power(a=1,b=1):
    return a**b

result=power(2,3)
print(result)

# positional argument -> follow sequence of order

# keyword argument

res=power(b=3,a=2)
print(res)